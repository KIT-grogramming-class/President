# 大富豪AI 改善内容

## 簡潔版

C++の `SimplePlayer` を、合法手を列挙して評価関数で最善手を選ぶヒューリスティックAIに改善した。

通常時は評価関数 `scoreMove()` で判断し、終盤だけ簡易モンテカルロシミュレーションを使って候補手を再評価する。モンテカルロは自分の手札が6枚以下、または相手の最小手札が3枚以下のときだけ使う。

また、`ai-tuning-lab` に実験用コピーを作り、評価関数の重みを自動対戦で調整する仕組みを追加した。進化的アルゴリズムでパラメータを探索し、短期戦の偶然で弱い値を採用しないように、長めの再評価を通った候補だけ保存するようにした。

終盤モンテカルロは `std::async` で並列化し、複数プレイアウトをマルチコアで処理できるようにした。

## 詳細

### 1. AI本体の方針

AI本体はC++のまま実装した。提出形式を壊さないため、基本的には `SimplePlayer` の思考ロジックを中心に変更している。

処理の流れは次の通り。

1. 現在出せる合法手を列挙する
2. 各手を `scoreMove()` で評価する
3. 通常時は最高点の手を選ぶ
4. 終盤だけ上位候補にモンテカルロ評価を追加する
5. 最終スコアが最も高い手を出す

### 2. 評価関数で見ている内容

`scoreMove()` では主に以下を評価している。

- 出した枚数が多いほど加点
- 弱いカードを処理できると加点
- 2、A、K、Jokerなど強いカードの雑な使用は減点
- ペアや3枚組を崩す手は減点
- 出した後の残り手札がきれいなら加点
- 終盤は強いカードを使ってでも上がりやすさを優先
- その手で上がれる場合は大きく加点
- 相手の残り枚数が少ない場合は防御寄りに補正

場が空のときは、弱い複数枚や弱い単体を処理しやすくした。場に返すときは、基本的に勝てる中で弱い手を出すようにした。

### 3. 終盤モンテカルロ

終盤だけ簡易モンテカルロシミュレーションを追加した。

発動条件は次のどちらか。

- 自分の手札が6枚以下
- 相手の最小手札枚数が3枚以下

使い方は、まず `scoreMove()` で候補手を上位3手程度に絞り、その候補だけを複数回プレイアウトする。相手の非公開手札は、自分の手札と見えているカード以外からランダムに仮定している。

本格的なISMCTSではなく、まず動くことを優先した簡易版の不完全情報モンテカルロである。

### 4. モンテカルロの合成方法

最終判断では、次を合成している。

- 既存の `scoreMove()` の点数
- モンテカルロでの勝率
- 残り手札の上がりやすさ

ただし、モンテカルロだけで判断を上書きすると弱くなりやすかった。そのため、現在は `scoreMove()` の1位を基本にし、基礎点が近い候補だけをモンテカルロで比較する保守的な形にしている。

### 5. 並列化

モンテカルロのプレイアウトは `std::async` を使って分割実行するようにした。

主な定数は次の通り。

- `kMcPlayouts`: 1候補あたりのプレイアウト回数
- `kMcThreads`: 並列実行数
- `kMcMaxPlies`: 1プレイアウトの最大手数
- `kMcWinRateWeight`: 勝率の重み
- `kMcOverrideWindow`: MC比較対象にする基礎点差
- `kMcOverrideThreshold`: MCで上書きするために必要な差

スレッド数を増やしすぎると起動コストで逆に遅くなるため、現状は控えめな並列数にしている。

### 6. パラメータ調整環境

元コードを直接壊さないように、`ai-tuning-lab` という実験用ディレクトリを作った。

中には以下がある。

- `sampleProgramForMac/`: C++対戦プログラムのコピー
- `tune.py`: パラメータ探索スクリプト
- `best-params.json`: 現在のベスト重み
- `known-good-params.json`: 強かった基準重み
- `results.csv`: 自動対戦ログ

`tune.py` は提出物ではなく、C++内の定数を自動で書き換えて `make` と自動対戦を回すための調整道具である。

### 7. 進化的アルゴリズム

評価関数の重みセットを1個体として、進化的アルゴリズムで探索できるようにした。

仕組みは次の通り。

- 個体: 評価関数の重みセット
- 適応度: `Simple1 - ThinkTA1`
- 選択: トーナメント選択
- 交叉: 親2個体の重みを混ぜる
- 突然変異: 一部の重みを少し変える
- エリート保存: 上位個体を次世代に残す

短い対戦だけだと運で結果がブレるため、各世代の上位候補だけを長めに再評価してから保存するようにした。

### 8. 操作方法

実験用ディレクトリで作業する。

```sh
cd /Users/kakiuchiakira/Code/class/cpp/President/ai-tuning-lab
```

まず現在のベストを確認する。

```sh
python3 tune.py --mode baseline --from-best --games 10000 --repeats 2
```

さらに強化したい場合は、現在のベストを親にして探索する。

```sh
python3 tune.py --mode genetic --from-best --games 500 --repeats 1 --verify-games 3000 --verify-repeats 2 --population 8 --generations 4 --seed 10
```

探索後に長めに確認する。

```sh
python3 tune.py --mode baseline --from-best --games 10000 --repeats 2
```

結果の `best_margin` または `margin` が `Simple1 - ThinkTA1` の差である。目標はこの値を安定してプラスにし、できれば10000戦で1000点前後以上の差を出すこと。

### 9. 確認した結果

モンテカルロ追加後、短めの確認では以下のような結果が出た。

- 1000戦: `Simple1 - ThinkTA1 = +171`
- 3000戦: `Simple1 - ThinkTA1 = +212`

ただし、1000戦や3000戦はまだブレがある。最終判断は10000戦以上で確認する必要がある。

### 10. 今後の改善案

まだ改善できる点はある。

- MC定数を進化的アルゴリズムでさらに調整する
- `kMcThreads` と `kMcPlayouts` のバランスを探す
- 相手プレイヤーごとの傾向をプレイアウト方針に反映する
- 簡易モンテカルロをISMCTS寄りに拡張する
- 終盤だけでなく、相手が危険な場面の防御評価をさらに強める
- 10000戦以上の結果を複数seedで確認して、偶然勝ちを避ける
